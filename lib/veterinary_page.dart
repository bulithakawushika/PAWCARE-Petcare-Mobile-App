import 'package:flutter/material.dart';

class VeterinaryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Veterinary'),
      ),
      body: const Center(
        child: Text('Veterinary Page Content'),
      ),
    );
  }
}
